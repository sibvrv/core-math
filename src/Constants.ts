/**
 * Commonly used constants
 */
const PI = Math.PI;

export const RADIANS_TO_DEGREES = 180.0 / PI;
export const DEGREES_TO_RADIANS = PI / 180;

export const PI2 = 2.0 * PI;
export const PI4 = 4.0 * PI;
export const PI_SQR = PI * PI;
export const PI_HALVES = 0.5 * PI;
export const PI_INV = 1.0 / PI;
export const PI2_INV = 0.5 / PI;

export const SQRT3 = Math.sqrt(3.0);
export const SQRT2_INV = 1.0 / Math.SQRT2;

export const LN2_INV = 1.0 / Math.LN2;
